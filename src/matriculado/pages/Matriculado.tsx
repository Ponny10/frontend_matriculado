export const Matriculado = () => {
  return (
    <div>Matriculado</div>
  )
};

/* Para trabajar con LazyLoad, es necesario exportar por default lo modulos o componentes */
export default Matriculado;
